<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.ecommerceanalysis.mapper.UserMapper">

    <select id="list" resultType="com.ecommerce.ecommerceanalysis.entity.User">
        select * from user
    </select>

    <insert id="add">
        insert into user(username,name,role,email,status,last_login)
        values(#{username},#{name},#{role},#{email},#{status},#{lastLogin})
    </insert>

    <update id="update">
        update user set username=#{username},name=#{name},role=#{role},email=#{email},status=#{status}
        where id=#{id}
    </update>

    <delete id="delete">
        delete from user where id=#{id}
    </delete>

</mapper>